#include <stdio.h>

/*
 * main - Entry point
 *
 * Return: 0 is success
 */
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
if (ch != 'e' && ch ch != 'q')
putchar(ch);
}
putchar('\n');
return (0);
}
